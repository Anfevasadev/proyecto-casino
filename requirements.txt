# -------------------------------------------
# requirements.txt
# Función:
#   Lista de dependencias Python del proyecto.
#   Se instala con: pip install -r requirements.txt
# Qué va aquí:
#   - Paquetes necesarios para ejecutar y desarrollar.
#   - Comentarios (#) explican por qué cada lib existe.
# -------------------------------------------

# API web (FastAPI) y servidor de desarrollo
fastapi==0.115.0        # capa HTTP (rutas, validación)
uvicorn[standard]==0.30.6  # servidor ASGI para correr la API

# Datos
pandas==2.2.2           # manejo de CSV/DFs, filtros y agregaciones

# Modelado / validación
pydantic==2.8.2         # modelos de entrada/salida (usado por FastAPI)

# Exportación de reportes
reportlab==4.0.7        # generación de archivos PDF
openpyxl==3.1.2         # generación de archivos Excel (.xlsx)

# Pruebas (básico; iremos afinando)
pytest==8.2.1           # framework de testing
httpx==0.27.2           # cliente HTTP para pruebas si se requiere

# Seguridad / Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4